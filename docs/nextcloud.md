# Nextcloud Deployment

## Prerequisites
- Docker and Docker Compose installed
- Traefik reverse proxy on `proxy` network
- Local SSL certificates configured
- DNS/hosts entry for `nextcloud.homelab.local`
